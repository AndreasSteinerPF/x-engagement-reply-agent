# Watched X authors -- 3 real handles, per the Phase 7 demo requirement
# (see docs/implementation-plan.md, Phase 7). The earlier placeholder
# entries (exampleauthor, ssafavi) have been removed; they never resolved
# on X and only added noise to real run output.
# Schema documented in docs/config-schema.md.

authors:
  - author: "Crypto.com"
    handle: "cryptocom"
    company: "crypto.com"
    aliases:
      handles: []
      authors: []
    # Temporarily deactivated for a clean single-match recording -- crypto.com's
    # content never scores near the corpus threshold anyway, so this only
    # removes an always-zero-subtask task from the demo's Asana project, not
    # any real coverage. Re-enable after recording.
    active: false

  - author: "Propy"
    handle: "propy"
    company: "propy"
    aliases:
      handles: []
      authors: []
    active: true

  - author: "Aleksa Zakic"
    handle: "aleksasazakikic"
    company: "personal"
    aliases:
      handles: []
      authors: []
    active: true
